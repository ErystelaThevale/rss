# RSS Relay Explorer 設計変更履歴

`DESIGN_NOTES.md`は**現在有効な設計のみ**を記す。このファイルには、設計変更の経緯・実装時に発見したバグ・レビューで指摘された不整合など、時系列の記録を残す。各節見出しに対応する`DESIGN_NOTES.md`の節番号を付記する。

---

## M1（初期実装）

取得層（HN/Reddit/Lemmyの購読フィードから新着を取得し、DeepLでタイトルを翻訳、CSVへ保存してStreamlit上に一覧表示する）を実装。フィード選定を絞り込むための照合・relay判定層は未着手。

このマイルストーンについての実装時の経緯・発見バグの詳細な記録は残していない（このDESIGN_HISTORY.md自体をM1完了後に新設したため）。以降の変更から時系列記録を開始する。

---

## M2（保存済みCSVの列名不整合によるクラッシュ）— 対応: §3・§4

2026-08-14。ツール起動時に`KeyError: "['title_translated'] not in index"`でクラッシュするとユーザーから報告。

**原因**: `data/rss_items.csv`（保存済みデータ）のヘッダーが`title_ja,title_en`という旧スキーマのまま残っていた一方、`core/acquisition.py`は`title_translated,title_original`という列名で保存する実装に既に変わっていた。`load_saved_items()`はCSVをそのまま読むだけで列名の変換をしないため、Fetchボタンを押す前に保存済みCSVを表示しようとした`ui/feed_tab.py`の`df[["source", "title_translated", ...]]`が存在しない列を要求してクラッシュしていた。

**確認**: CSVの中身自体は列の意味（`title_ja`＝翻訳後、`title_en`＝原文）が新スキーマと同じ並び順で対応していたため、データの再取得・再翻訳は不要と判断。

**修正**: `data/rss_items.csv`のヘッダー行のみを新しい列名に付け替えた（データ本体は変更なし）。

**再発防止（M3で追加実装）**: この時点では個別データの修正のみに留めたが、同種の問題（コード側のスキーマ変更に保存済みCSVが追従していない）は今後も起こりうるため、M3で表示側の列選択を`reindex`ベースに変更し、列不足そのものではクラッシュしなくなるよう恒久対策した（§4参照）。

---

## M3（コメント数の取得・Redditの対象外化・HNリンクの修正、DESIGN_NOTES.md新設）— 対応: §1・§2・§3・§4

2026-08-14、M2と同日。ユーザーから「現在のコメント数を、スレッドの活性度の指標として拾いたい」との要望。あわせて「HNのlinkが記事本体でなく記事に埋め込まれた先のリンクになっている、HN本体のスレッドへのリンクにしたい」との指摘。

**調査（実装前に3プラットフォームの生RSSを実際に取得して確認）**:
1. **HN（hnrss.org）**: `description`に`Points: N`・`# Comments: N`という定型行が入っており、コメント数は取得可能と判明。あわせて`entry.link`が記事本体（外部URL、"Article URL"）を指し、HN本体のスレッドURLは別フィールド`entry.comments`（feedparserの`<comments>`タグへのマッピング）に入っていることを確認——ユーザーが指摘した「linkが記事に埋め込まれた先になっている」不具合の原因はこれだった。
2. **Lemmy**: `description`（CDATA、投稿本文込み）に`N points | <a href="...">N comments</a>`という形式でコメント数が埋め込まれていることを確認。本文中に数字が現れうるため、`|`直後のアンカー要素に絞った正規表現でないと誤検出しうると判断。
3. **Reddit（未認証`.rss`）**: フィード全体を確認したが、コメント数を示すフィールドが一切存在しないことを確認（`entry.link`はコメントページを指しているが件数の記載はない）。取得するには認証付きAPI（OAuth）か投稿ごとの追加リクエストが必要になり、「RSSを軽く取得するだけ」という設計方針から外れるため、**ユーザー方針によりReddit分は対象外**と確定した。

**実装内容**:
- `core/fetch_feed.py`: `FeedItem`に`comments: Optional[int] = None`を追加。`_extract_link(platform, entry)`を新設し、HNのみ`entry.comments`（HN本体URL、欠落時は`entry.link`にフォールバック）を`link`として採用、Lemmy/Redditは従来どおり`entry.link`（いずれも投稿＝コメント欄ページを指すことを確認済み）。`_extract_comment_count(platform, entry)`を新設し、HN/Lemmyはそれぞれの定型パターン（`_HN_COMMENTS_RE`/`_LEMMY_COMMENTS_RE`）で`entry.summary`から抽出、Redditは常に`None`。
- `core/acquisition.py`: `_COLUMNS`に`comments`を追加し、`rss_items.csv`へ保存。
- `ui/feed_tab.py`: 表示列に`comments`（`NumberColumn`）を追加。あわせて、M2の再発防止として、列選択を`df[[...]]`（列不足でKeyError）から`df.reindex(columns=display_columns)`（列不足はNaN補完で表示継続）に変更。
- `DESIGN_NOTES.md`・`DESIGN_HISTORY.md`を本ツール向けに新設（ユーザー要望。oasbpプロジェクトの構成・書式を踏襲）。

**検証**: HN/Lemmy/Redditそれぞれ実際に`fetch_feed_items()`を呼び出し、HNの`link`がnews.ycombinator.com側になっていること・`comments`が実際の値（HN: 6, 33, 2...／Lemmy: 0, 9...）を返すこと・Redditの`comments`が`None`のままであることを確認済み。既存の`rss_items.csv`（旧スキーマ、`comments`列なし）に対しても`reindex`でクラッシュしないことを確認済み。

**残作業**: 既存`rss_items.csv`の内容（旧HN行のlinkが記事URLのまま、comments列が空）は次回Fetch実行時に新スキーマへ全件更新される。DeepL翻訳コストが発生するため、この変更のタイミングでは実Fetchを実行していない。

---

## M4（Lemmy購読リストの偏り解消: カテゴリミスマッチの是正・未収録分野の追加）— 対応: `data/rss_feeds.csv`

2026-08-14、M3と同日。ユーザーから「Lemmyのコミュニティがかなり偏っている気がするが、リストの問題か」との指摘。

**調査（実装前にLemmy APIで購読中5コミュニティの規模を実測）**:
- `philosophy@lemmy.world`（購読1,896・日次アクティブ3人）に対し、`technology@lemmy.world`（購読87,151・日次アクティブ5,354人）・`politics@lemmy.world`（購読30,777・日次アクティブ3,162人）は桁違いの規模だった。
- あわせて、1回のフェッチが直近20件固定（コード側でpage/limitパラメータを使っていない）であることも影響を増幅していた：高頻度コミュニティは20件が数時間〜1日未満で埋まり、`days_back=7`の窓に対してはむしろ実際の週間投稿量から大幅に間引かれる形になる一方、低頻度のphilosophyは20件で2.5ヶ月分——という状態で、両者の性質の違いが最終テーブル上の「技術/政治で埋め尽くされ、philosophyがほぼ見えない」偏りとして現れていた。
- ユーザーに提示の上、対応方針を確認：(1) 偏りをそのまま許容、(2) philosophyの代替コミュニティ探索、(3) 表示側で1コミュニティあたりの件数上限、(4) 他分野も含めて規模を一通り棚卸し、の4択から「他に関係のあるコミュニティを調査して追加したい」を選択。

**カテゴリミスマッチの発見**: 棚卸しの過程で、`technology@lemmy.world`が「Computer Science, Artificial Intelligence」に分類されていたが実際は無差別の一般テックニュース、`politics@lemmy.world`が「Political Science」に分類されていたが実際は米国内政治の一般ニュースであることが判明（投稿タイトルを実際に確認して確定）。加えて、Sociology・Psychology・EconomicsはLemmy側に1件も無く（Redditのみ）、`rss_feeds.csv`のカテゴリ列と実際の購読内容が最初から一部食い違っていたことも判明した。

**候補選定**: Lemmy検索API（`/api/v3/search?type_=Communities`）でカテゴリごとに候補コミュニティを検索し、購読者数・総投稿数・日次/月間アクティブ数を比較。さらに候補ごとに実際に`fetch_feed_items`を呼んで**直近7日以内の件数**・**直近20件が何日分に相当するか**を実測した——月間アクティブ数が高くても投稿頻度（原著投稿数）が低いコミュニティ（`fosai@lemmy.world`: 7日以内0件、`psychology@lemmy.world`: 7日以内0件）があることが実測で判明し、購読者数・アクティブ数だけでは選定の根拠にならないと確認したため。

**確定した変更**（`data/rss_feeds.csv`）:
| 旧 | 新 | 理由 |
|---|---|---|
| `lemmy.world/c/philosophy`(月間アクティブ79) | `lemmy.ml/c/philosophy`(月間アクティブ175) | より活発な同分野コミュニティへ差し替え |
| （未収録） | `mander.xyz/c/sociology`(新規) | Sociology分野がLemmy側に無かったため追加 |
| （未収録） | `mander.xyz/c/appliedpsychology`(新規) | Psychology分野がLemmy側に無かったため追加。`psychology@lemmy.world`は月間アクティブ199だが実測で7日以内0件だったため、実測2件だったこちらを採用 |
| （未収録） | `lemmy.ml/c/economics`(新規) | Economics分野がLemmy側に無かったため追加 |
| `lemmy.world/c/science`(維持) | 変更なし | History & Philosophy of Science特化のコミュニティが見つからなかったため、最も近い一般scienceを維持 |
| `lemmy.world/c/technology`（カテゴリ誤り、87,151購読） | `programming.dev/c/Aii`（336購読・月間アクティブ403、実測で7日以内5件） | AI特化かつ実測で最も安定した頻度。`fosai`(4,795購読)は購読者数こそ多いが実測7日以内0件だったため不採用 |
| `lemmy.world/c/politics`（カテゴリ誤り、30,777購読） | `lemmy.ml/c/geopolitics`（431購読・実測で7日以内3件） | Political Science寄りの内容かつ巨大ハブの制圧を解消 |
| `lemmy.ml/c/asklemmy`(維持) | 変更なし | |

**効果**: 変更後、7日以内の取得件数は各コミュニティ1〜18件の範囲に収まり（旧: 1〜20件で20が2コミュニティを占有）、単一コミュニティが表を埋め尽くす状態は解消。全8URLについて実際に`fetch_feed_items()`が正常に取得できることを確認済み。

**既知の残課題**: Logic・Cybernetics・History & Philosophy of Science（専門特化）に該当する実用的な活動量のLemmyコミュニティは検索の結果見つからなかった（Reddit側では`r/logic`が既存）。Philosophy・Sociologyは差し替え後も実測7日以内1件程度と依然薄く、Lemmy生態系側の限界（該当分野の絶対的な活動人口の少なさ）であり、購読先の選定では解消しきれない。

---

## M5（Redditの取得失敗が偏って見える原因の特定・結合フィード化による解消）— 対応: §1・§2.1・§5

2026-08-14、M4と同日。ユーザーから「Redditがかなりの頻度で取得できないのは、サブレ自体の特性かReddit自体か。取得失敗するサブレが偏っている気がする」との指摘、および「自分のアカウントでカスタムFeedを作っても変わらないのか」との質問。

**調査1（原因の特定）**: 本番と同じ`FETCH_DELAY_SEC=15秒`間隔で、購読中の17サブレへ実際に順次リクエストを送り、HTTPステータス・応答時間を記録した。結果、成功（200）のタイミングが t=0, 45, 105, 165, 225秒——**45〜60秒に1回**という明確な周期で現れ、どのサブレを叩いているかとは無関係だった。あわせて429応答は一律0.2秒（エッジ側で即座に弾かれている）、200応答は0.6〜1.0秒（実サーバーまで到達）という応答時間の違いも確認。**結論: サブレ自体の特性ではなく、IPごとの時間窓ベースのレート制限**（どのサブレを叩いても関係なく45〜60秒に1リクエストしか通らない）であり、失敗するサブレが偏って見えるのはリクエスト順序上たまたまその時刻に叩かれた結果に過ぎないと判明。

**調査2（カスタムFeedの効果）**: ログイン不要の複数サブレ結合URL（`r/sub1+sub2+.../new/.rss`）を試したところ、17サブレを1リクエストに束ねただけで**1回で成功**した（個別だと12/17失敗）。old Reddit の`.rss`インターフェースはログイン有無に関係なく同じ配信経路と考えられ、効いているのは「アカウントを作ったこと」ではなく「複数サブレを1リクエストに束ねてリクエスト総数を減らしたこと」だと判断。ユーザー自身のアカウントでカスタムマルチレディットを作っても、内部的には同じ「複数サブレ→1フィード」の仕組みであり、無料でログイン不要の結合URLと比べて追加の恩恵は無いと結論した（真偽は未検証だが、old Reddit RSSが認証状態でレート制限の扱いを変えるという情報は見当たらない）。

**調査3（結合時のソート順）**: 結合URLの`sort`パラメータによって、25件の枠を17サブレがどう分け合うかを比較した。
| sort | 収録サブレ数 | 偏り |
|---|---|---|
| `new`（時系列） | 8/17 | LocalLLaMAが25件中10件（40%）を占有 |
| `top`（週間） | 4/17 | Economicsが25件中14件（56%）を占有——`new`より悪化 |
| `hot`（Reddit既定のホットランキング） | **15/17** | 最大でも3件、大半が1〜2件と分散 |

`hot`はスコア（≒upvote＋コメント数を含む活性度）と経過時間を各コミュニティ内で正規化してランキングするため、結合フィードでも投稿頻度の高いサブレだけに支配されにくいと判明。ユーザーから「コメント数を直接取れなくても、hotソート自体が活発度の代替になるのでは」との指摘があり、その通りだったことを確認。

**調査4（`limit`パラメータ）**: `hot`ソートに`limit=100`を追加したところ、1リクエストで100件取得でき、**17/17サブレ全てが出現**した（`compsci`・`systems`のような低頻度サブレも1〜2件は含まれる）。

**実装方針の確定（ユーザー提案）**: 「翻訳コストを下げるために件数を絞っているなら、hot取得→日付フィルタ→サブレ単位の件数フィルタ→出力、の順で内部メモリ上で済ませればよいのでは」との提案を受け、その通りに実装:
1. `rss_feeds.csv`のReddit全17行は維持（`label`・`category`はサブレごとに個別のまま）、`url`列だけを全行で同一の結合URL（`hot`・`limit=100`）に統一。
2. `core/acquisition.py`に`_group_targets_by_url`を新設。同一urlを持つ行を1グループにまとめ、**グループ単位で1回だけfetchする**（Redditの17行→1リクエスト）。
3. 結合フィードから返る各アイテムは、`fetch_feed_items`の時点では暫定labelを持つため、`_resolve_reddit_label`で`item.link`からサブレ名を正規表現で逆引きし、対応する行の正しい`label`へ差し替える。
4. 日付フィルタの後、翻訳（DeepL呼び出し）の前に、Redditのみサブレ単位で件数上限`REDDIT_MAX_PER_SUBREDDIT`（既定5、`core/config.py`）をかける。`hot`順のまま先頭からN件を残すため、事実上「サブレごとの上位N件（hot順）」になる。
5. `core/fetch_feed.py`の429リトライを、固定1回（`RETRY_ON_429_WAIT_SEC=20秒`）から**最大3回・50秒間隔**（`RETRY_ON_429_MAX_ATTEMPTS`）のループに変更——調査1で判明した45〜60秒周期に対し20秒では足りず、リトライ自体も429になるケースを確認したため。Reddit分がリクエスト1本に集約された分、多少待ってでも確実に成功させる方を優先する判断。

**検証（翻訳を伴わずパイプライン全体を実行、DeepLコストをかけずに確認）**: 全26行が10フェッチグループに集約（Reddit 17行→1グループ）されること、Redditの結合フィードが1リクエストで100件取得できること、日付フィルタ後・サブレ単位上限後も**17/17サブレが最終結果に残る**こと（実測: 各サブレ1〜5件）、他プラットフォーム（HN 20件・Lemmy計48件）と合わせて最終128件のうちどのソースも突出しない構成になることを確認済み。

**効果**: リクエスト総数26→10、うちRedditは17（12件が429で失敗）→1（100%成功）。失敗率・偏りの両方を1つの変更で解消。

**既知の残課題**: `REDDIT_MAX_PER_SUBREDDIT=5`は初期値としての判断（HN 1フィード分・Lemmy各コミュニティの実測件数と概ね同スケールになるよう設定）であり、運用しながら調整の余地がある。ユーザーのアカウントでの認証付きカスタムマルチレディットがレート制限を実際に緩和するかは未検証（検証には実アカウントでの認証が必要なため保留）。

---

## M6（翻訳のオプション化・Reddit件数上限のUI化）— 対応: §1・§2.1・§2.2・§4

2026-08-14、M5と同日。「RSSツールは公開の価値があるか」という一連の議論（Upvote RSSとの比較、Reddit公式APIの承認ハードルの高さの実地調査）を経て、ユーザーから「翻訳機能をオプション化できないか、ついでに各サブレの取得上限数も指定できるようにしてほしい」との要望。

**実装内容**:
- `core/acquisition.py`: `run_acquisition(days_back, translate=True, reddit_max_per_subreddit=REDDIT_MAX_PER_SUBREDDIT)`にシグネチャ変更。`translate=False`の場合はDeepL（`translate_titles`）を一切呼ばず、`title_translated`に`title_original`をそのまま代入する（この経路に限り`DEEPL_TOKEN`未設定でも動作する）。`REDDIT_MAX_PER_SUBREDDIT`定数への直接参照だった上限チェックを、引数`reddit_max_per_subreddit`を使うよう変更（既定値は従来通り定数を使うため、呼び出し側を変更しない限り挙動は変わらない）。
- `ui/feed_tab.py`: 「Days back」の隣に「Translate titles」チェックボックス（既定値`bool(DEEPL_TOKEN)`——トークン未設定の環境では自動的にOFFから始まり、翻訳をしない構成でもエラーなく起動できる）と「Reddit: max/sub」数値入力（既定`REDDIT_MAX_PER_SUBREDDIT`）を追加。いずれもFetch時に`run_acquisition`へそのまま渡す。
- `core/__init__.py`: `REDDIT_MAX_PER_SUBREDDIT`をpackage直下からimportできるよう追加（UI側で既定値表示に使うため）。

**検証**: `translate=False`でHN分のみ実際に取得し、`title_translated == title_original`が全行で成立すること・DeepL API呼び出しが発生しないこと（実行時間からも確認、DEEPL_TOKENが実際に設定されている環境で試験したが、この経路ではAPIに触れていない）を確認済み。

**位置づけ**: M5までで「翻訳前に安く絞り込む」パイプライン自体は完成していたが、絞り込みの度合い（サブレ上限）と翻訳の要否は運用者ごとに好みが分かれるパラメータのため、コード変更なしでUIから調整できるようにした。「公開するなら翻訳を切り離せばフリーになるのでは」という当初の発想（本DESIGN_HISTORY.mdには残していない会話上の議論。結論は「翻訳はむしろ既存の類似OSSに対する数少ない差別化点であり、切り離すこと自体に価値は薄い」だった）とは独立に、**個々の運用者が自分の状況に応じてON/OFFできる設定項目として実装**した——DEEPL_TOKEN取得の有無に関わらずツールが起動できる、という運用上の実利がある。
